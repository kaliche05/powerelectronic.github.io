function irAEmpresa() {
    // Utiliza la función scrollIntoView para desplazarte a la sección con id "seccionDestino"
    document.getElementById('seccionEmpresa').scrollIntoView({ behavior: 'smooth' });
}

function irAMantenimiento() {
    // Utiliza la función scrollIntoView para desplazarte a la sección con id "seccionDestino"
    document.getElementById('seccionMantenimiento').scrollIntoView({ behavior: 'smooth' });
}

function irAPreguntas() {
    // Utiliza la función scrollIntoView para desplazarte a la sección con id "seccionDestino"
    document.getElementById('seccionPreguntas').scrollIntoView({ behavior: 'smooth' });
}

function irAContacto() {
    // Utiliza la función scrollIntoView para desplazarte a la sección con id "seccionDestino"
    document.getElementById('seccionContacto').scrollIntoView({ behavior: 'smooth' });
}

function irAInicio() {
    // Utiliza la función scrollIntoView para desplazarte a la sección con id "seccionDestino"
    document.getElementById('seccionInicio').scrollIntoView({ behavior: 'smooth' });
}